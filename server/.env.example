DATABASE_URL="postgresql://jrl@localhost:5432/media_viewer_dev"
MEDIA_ROOT="/Users/jrl/projects/slides/media_viewer/data"
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
COOKIE_SECRET="replace-with-a-random-32+-char-secret"
PORT=3100
SESSION_TTL_DAYS=30
HTTPS_ENABLED=false
# Required when HTTPS_ENABLED=true. Generate with:
#   openssl req -x509 -newkey rsa:2048 -nodes -keyout key.pem -out cert.pem -days 825 \
#     -subj "/CN=<hostname>" -addext "subjectAltName=DNS:<hostname>,IP:<host-ip>"
# TLS_KEY_PATH="/path/to/key.pem"
# TLS_CERT_PATH="/path/to/cert.pem"
